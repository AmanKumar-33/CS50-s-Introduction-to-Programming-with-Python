# writing an code for the programme where program in Python that prompts the user 
# for input and then outputs that same input, replacing each space with ... 
# (i.e., three periods).

voice = input(" ")
voice = voice.replace(' ','...')
print(voice)